# Optional; provider credentials can instead be saved in JevZero's Connections form.
TYPESAFE_API_KEY=
TYPESAFE_MODEL=jev-latest
GMAIL_CLIENT_ID=
GMAIL_CLIENT_SECRET=
# Register http://127.0.0.1:3000/oauth/callback for the default launcher.
# Optional absolute path for your private local workspace:
# JEVZERO_LOCAL_DATA_DIR=/path/outside/repository/jevzero-data
# Internal storage/session/API secrets are generated automatically. Do not set them here.
