# Hosting decision

JevZero now runs locally by default. No Render or Vercel project is needed. The previous hosted API, owner-password flow, and deployment manifests have been retired.

Use [the local setup guide](local.md). The current app automatically trusts its local OS user and is deliberately unsuitable for public hosting without a new authentication and isolation design.
