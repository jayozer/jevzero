# No manual frontend environment configuration is needed.
# Use `uv run jevzero` from the repository root to launch both local servers.
# `npm run dev` by itself serves the synthetic demo without Gmail/key saving.
